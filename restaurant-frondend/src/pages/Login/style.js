import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    authContainer: {
        marginTop: 32,
    },
    authForm: {
        marginTop: 24,
        marginBottom: 24,
    },
    authTitle: {
        fontWeight: 'bold',
        marginBottom: 24,
    },
    authInput: {
        marginBottom: 24,
    },
});

export default useStyles;